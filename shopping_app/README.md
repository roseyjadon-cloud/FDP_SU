# Shopping App — Login Page

Open `login.html` in a browser to view the login form.

Quick local server (recommended) from the `shopping_app` folder:

```powershell
# Python 3
python -m http.server 8000

# then open http://localhost:8000/login.html
```

Demo credentials for the mock auth in `login.js`:

- Email: `user@example.com`
- Password: `password`

Replace the mock logic in `login.js` with real API calls for production.
